{
  "name": "@ecomflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, enumerations et regles metier partagees entre l'API et le front EcomFlow",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "test": "jest --passWithNoTests"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "@types/node": "20.19.9",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.9.3"
  }
}
